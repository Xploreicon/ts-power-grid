{
  "name": "@ts-power-grid/mqtt-ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Ingests T&S gateway telemetry from the MQTT broker into Supabase.",
  "main": "index.ts",
  "scripts": {
    "start": "tsx --tsconfig ./tsconfig.json index.ts",
    "dev": "tsx watch --tsconfig ./tsconfig.json index.ts",
    "dry": "tsx --tsconfig ./tsconfig.json index.ts --dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.103.3",
    "mqtt": "^5.15.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
